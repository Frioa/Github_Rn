/**
 * Trending 搜索时间跨度mo
 * @param showText
 * @param searchText
 * @constructor
 */
export default function TimeSpans(showText, searchText) {
    this.showText = showText;
    this.searchText = searchText;
}